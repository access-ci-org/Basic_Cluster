127.0.0.1 localhost
::1 localhost

{% for host in groups['headnode'] %}
{{ hostvars[host]['ansible_host'] }} {{ hostvars[host]['inventory_hostname'] }}  {{ hostvars[host]['inventory_hostname'] }}.local
{% endfor %}
